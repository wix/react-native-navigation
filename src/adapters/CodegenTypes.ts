// Keep the codegen annotations available on RN 0.77+, without importing
// private React Native paths removed by the RN 0.87 Strict TypeScript API.
// Codegen recognizes these imported names and preserves their native types.
export type Double = number;
export type UnsafeObject = object;
